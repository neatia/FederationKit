import Granite
import SwiftUI
import Foundation

extension Home: View {
    
    public var view: some View {
        VStack {
            
        }
        .padding(24)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
